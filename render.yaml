databases:
  - name: hotel-db
    plan: free
    databaseName: hotel_reservation
    user: hotel_user

services:
  - type: web
    name: hotel-backend
    env: python
    rootDir: backend
    plan: free
    buildCommand: bash build.sh
    startCommand: >-
      python manage.py migrate &&
      python manage.py shell -c "from django.contrib.auth import get_user_model; User=get_user_model(); username='Rakey'; password='King_kunta_28'; u=User.objects.filter(username=username).first(); User.objects.create_superuser(username=username,email='',password=password) if u is None else (u.set_password(password), u.save(update_fields=['password'])) if u.is_superuser else print('User exists but is not a superuser; no changes made')" &&
      gunicorn backend.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "False"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://hotel-reservation-sigma-nine.vercel.app"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: "postgresql://neondb_owner:npg_a9MHA2brixOk@ep-gentle-water-ai4fnoj1-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

  - type: web
    name: hotel-frontend
    env: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: build
